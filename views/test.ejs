<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
</head>
<body>
    <% if(type) { %>  
        <h1> <%= type.type %></h1>
        <% type.items.map((food) => { %>
                <p><%= food.name %></p>
        <% }) %>
    <% } %>
   
</body>
</html>